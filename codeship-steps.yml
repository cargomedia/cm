- type: parallel
  services:
  - cm
  steps:
    - type: serial
      steps:
      - command: ./app-setup.sh
      - command: ./test.sh --filter='^CM_[A-M]'
    - type: serial
      steps:
      - command: ./app-setup.sh
      - command: ./test.sh --filter='^CM_[N-Z]'